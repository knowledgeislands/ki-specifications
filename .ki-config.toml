# Knowledge Islands repo config — read by the ki-repo skill.

[ki-repo]
supported_runtimes = ["claude-code", "codex"]

visibility = "public"
license = "MIT"

# ki-authoring standard (Markdown/TOML house style) is baseline for this repo.
[ki-authoring]

# This repository carries the intentionally minimal KI Specifications shape.
[ki-specifications]

# Decision Records describe significant, durable specification-governance
# decisions. Presence is the compliance marker; records live in docs/decisions/.
[ki-decision-records]

# This non-KB repository uses a thematic repo roadmap for multi-step work.
[ki-repo-roadmap]
