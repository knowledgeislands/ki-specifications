# Handover

REPLACE — prose expansion of the `handover` object in `manifest.json`: what the next person or agent needs to know to resume this package, and where to pick it up.

## Context

REPLACE — the state of the package at handover time.

## Resume points

- REPLACE first resume point.
